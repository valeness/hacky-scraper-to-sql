hacky-scraper-to-sql
====================

In the most hacky way, this scrapes web content and puts it into an SQL uploadable format. This was a one off script I needed for work, don't expect it to be nice.
